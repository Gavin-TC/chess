#include <bot.h>

// NONE OF THIS WORKS
// AVOID VV
/*
char* getBotMove(Piece** pieces) {
  Piece** bot_pieces = calloc(16, sizeof(Piece*));
  int bot_index = 0;

  for (int i = 0; i < 32; i++) {
    if (pieces[i]->color == is_white ? "WHITE" : "BLACK") {
      bot_pieces[bot_index] = pieces[i];
      bot_index++;
    }
  }

  int piece_index = rand() % 17;

  int file1 = bot_pieces[piece_index]->pos.x;
  int rank1 = bot_pieces[piece_index]->pos.y;

  printf("(x: %d, y: %d)\n", file1, rank1);

  return 'a';
}

int numberToLetter(int num) {
  int converted_file = ' ';

  switch (num) {
    case 0:
      converted_file = 'a';
      break;
    
    case 1:
      converted_file = 'b';
      break;
    
    case 2:
      converted_file = 'c';
      break;

    case 3:
      converted_file = 'd';
      break;

    case 4:
      converted_file = 'e';
      break;

    case 5:
      converted_file = 'f';
      break;

    case 6:
      converted_file = 'g';
      break;
    
    case 7:
      converted_file = 'h';
      break;
    
    default:
      converted_file = 'z';
      break;
  }
  return converted_file;
}
*/
